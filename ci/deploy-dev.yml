---
platform: linux
inputs:
  - name: django-git
image_resource:
  type: registry-image
  source: { repository: alpine}
run:
  path: /bin/sh
  args:
    - -c
    - |
      apk add py-pip python-dev libffi-dev openssl-dev gcc libc-dev make

