---
platform: linux
inputs:
  - name: django-git
image_resource:
  type: registry-image
  source: { repository: python, tag: 3}
run:
  path: /bin/sh
  args:
    - -c
    - |
      pip install -r django-git/requirements.txt
      python django-git/mysite/manage.py test

